<h4>Lista de Gerentes</h4>
<p></p>
<div class="table-responsive">
  <table id="tb-user" class="table table-striped table-hover">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Telefone</th>
        <th>Email</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each users}}
          {{#compare type 'Gerencia'}}
            <tr>
              <th onClick="location.href='/users/producers/{{_id}}'" class="th-users"> {{fullname}}</th>
              <th>{{cellphone}}</th>
              <th>{{email}}</th>
              <th>
                <button onClick="location.href='show/{{_id}}'" class="fa fa-info btn"></button>
              </th>
            </tr>
          {{/compare}}
      {{/each}}
    </tbody>
  </table>
</div>

<form class="user-edit-button" action='/users/show/{{user._id}}' method="GET">
  <button class="btn ml-2" type="submit"> Adicionar </button>
</form>
